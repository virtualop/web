<div id="dev-wrap" class="container-fluid">
    <div id="header-wrap">
      <div id="title-wrap" class="float-left">
        <h1>dev</h1>
      </div>
      <div id="buttons" class="float-right">
        <a href="/dev/refresh" data-turbolinks="false">refresh</a>
      </div>
  </div>

  <div>
    <% @working_copies.each do |working_copy| %>
    <%= render partial: "working_copy", locals: { :working_copy => working_copy } %>
    <%= render partial: "working_copy_detail", locals: { :working_copy => working_copy } %>
    <% end %>
    <%= render partial: "commit_modal" %>
  </div>
</div>
